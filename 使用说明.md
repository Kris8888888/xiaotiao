# 小条

一个macOS菜单栏小应用。在桌面上用一条置顶、半透明、不抢焦点的小条实时显示你自选的A股和美股价格与涨跌幅。做得尽量不显眼：鼠标穿透、一键隐藏、可以伪装成时钟或一串数字。

## 安装
1. 打开 `小条.dmg`，把「小条」拖进「应用程序」。
2. 第一次打开：**右键点应用 → 打开**（不要双击）。系统说「无法验证开发者」，再点一次「打开」。之后正常双击即可。
   - 如果只有「移到废纸篓」：先点「完成」，再到「系统设置 → 隐私与安全性」底部点「仍要打开」。
3. 打开后菜单栏右上角多一个小横条图标，桌面右上角出现浮窗。它不占Dock。

## 浮窗
- 每只股票显示：代号（可关）、现价、涨跌幅。涨跌用▲▼箭头或+−正负号标，可选。默认单色，最不显眼。
- **拖动**：按住小条直接拖到任何位置，会记住。
- **鼠标穿透**（可选，菜单栏图标或设置「隐蔽」页开）：开了之后点它点不到，下面的窗口照常用；这时按住Option（⌥）再拖依然能挪位置。
- 它盖在所有窗口和全屏应用之上，所有桌面空间都在。

## 小条上能做的事（不穿透时）
- 鼠标停在某只股票上：浮出昨收、今开、最高最低、行情时间、是否交易中。
- 点一下某只股票：浏览器打开它的行情页（可在「外观 → 动态」关掉）。
- 右键：直接弹出菜单，不用去菜单栏。
- 价格变化时数字会轻轻一闪；休市的市场自动变暗；某只数据许久没更新时，后面出现一个小空心点。

## 价格提醒
设置 → 股票，每只后面两格「高于」「低于」填价格。现价碰到就发一条系统通知、小条上那只亮一下、响一声（可关）。价格回到区间内后再碰到会再提醒一次。第一次填的时候系统会问是否允许通知，选允许。

## 会议 / 投屏时自动隐藏
腾讯会议、Zoom、飞书会议、Keynote、Teams到前台时小条自动藏起来，切回别的应用自动恢复。名单在设置 → 隐蔽里改，填应用名字的一部分即可。

## 一键隐藏
- 快捷键 **⌃⌥S**（Control + Option + S）在任何应用里都能按，按一下隐藏，再按恢复。
- 也可以点菜单栏图标里的「隐藏小条 / 显示小条」。

## 伪装
菜单栏图标 → 「伪装」，或设置里的「隐蔽」页：
- **时钟**：浮窗只显示时间和星期。鼠标停在上面（或按 **⌃⌥D**）才露出行情，移开又变回时钟。
- **纯数字**：去掉名字、箭头和百分号，只剩「4.60 0.31 | 1251.43 -0.45」这样一串数。

## 设置（菜单栏图标 → 设置…，或按 ⌘,）
**股票**：输入代码或名字添加。A股填6位数字（600519、000001、510300都行，程序自动判断沪深），美股填字母（AAPL、TSLA）；也可以直接打「茅台」「apple」，从候选里点。每只可以起代号，浮窗上就显示代号。右侧上下箭头调整顺序，减号删除。交易时段刷新间隔默认1秒（行情源本身每3秒更新一档，1秒轮询是为了尽快拿到新的一档），休市60秒。接口连续失败会自动放慢到3 / 10 / 30秒，恢复后回到设定值。

**外观**：动态（价格变化轻闪、休市变暗、点击开行情页、提醒音）；大小70%～200%、底色不透明度、圆角；底（墨玻璃 / 纸白 / 无底）；涨跌颜色（单色 / 红涨绿跌 / 绿涨红跌）；字体（系统 / 圆体 / 衬线 / 等宽）；横条或竖列；边框、阴影；名字显示方式（不显示 / 代号 / 真名）；涨跌标记（▲▼箭头 / +−正负号 / 不标）、涨跌额、小数位。所有改动即时反映在浮窗上。

**隐蔽**：鼠标穿透开关；伪装模式；悬停露出；两个全局快捷键（显示隐藏、露出收回）可从预设里选；会议 / 投屏应用名单与自动隐藏开关；打开应用时先不显示。

**通用**：登录时自动打开；把浮窗放回右上角；最近一次行情错误。

## 数据
行情来自腾讯与东方财富的公开接口，直连不走代理，不需要账号。A股是交易所快照，通常3秒左右延迟；美股盘中基本实时，盘前盘后停在收盘价。A股9:15–15:00、美股9:30–16:00（纽约时间）算交易时段，其它时间自动放慢刷新。

## 常见问题
- **浮窗显示「—」**：行情没拉到。看菜单栏图标第一行的状态；一般是断网，或代码填错。
- **拖不动**：穿透开着。按住Option拖，或在菜单栏点「解除穿透」。
- **快捷键没反应**：可能和别的应用冲突，到设置「隐蔽」里换一个预设。
- **想彻底看不见**：⌃⌥S隐藏，或菜单栏「退出」。
- **换电脑**：把dmg拷过去装即可，设置不跟着走。

## 从源码编译
```
cd App && ./build.sh
```
只需要macOS命令行开发工具，不需要Xcode。产物在 `App/build/`（.app、.zip、.dmg）。
